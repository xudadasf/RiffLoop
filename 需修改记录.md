1、.gp模式当前播放的节所在行应该位于显示区域的中间，不然后面看不到了

2、暂停按钮有些情况下会失效
3、我希望区间循环的退出逻辑是在暂停的时候显示退出循环的按钮，点击即可退出区间循环模式

4、节拍器的强、次强、普通声音区分度有点低

5、在开启内嵌伴奏的时候，暂停按钮控制的应该是整个谱子和伴奏的音频啊而不是只关闭伴奏

6、PDF模式主页面的控制按钮点一下变成了节拍器，再点一下才进入设置页面，设置页面到PDF的透明度可以增强一些，进入设置页面后就退不回PDF显示页面了，这里不对，而且这里如果播放伴奏的话还需要伴奏的相关控制按钮，节拍器和伴奏，应该是分开的，这样能自己自定义节拍器第一拍和伴奏对齐点，当然默认的应该仍然是第一拍是伴奏起始位置

7、.gp 当前音光标稍微变细，但仍需明显；光标经过音符时应能同时看清光标和后面的音符，不能完全遮挡。

## 统一核查记录（2026-08-21）

- 1：真机已确认当前行位于显示区域中部，后续行可见。
- 2、3、5：播放和整组暂停本身已生效；真机发现 alphaTab 的延迟状态事件会覆盖按钮状态，已定位到运输状态同步，等待最终包统一复测按钮、退出循环和内嵌伴奏暂停。
- 4：强/次强/普通已按音高、响度和衰减三维拉开，参数及单元测试通过；最终包需真机听感确认。
- 6：PDF 单击控制、返回 PDF、半透明面板、伴奏/节拍器独立控制和第 1 拍对齐已完成编译与测试，等待最终包统一真机复测。
- 7：当前 `.at-cursor-beat` 使用不透明蓝色并带描边/光晕，经过音符时会遮挡；最小修正为 2 px 半透明蓝线，保留轻微白边和蓝色光晕以确保清晰。
