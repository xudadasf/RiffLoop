# 本地生成缓存清理状态

0.25.56 复核期间：按用户再次授权，使用 PowerShell 原生命令尝试清理当前项目 `output`（含绝对路径、Git 跟踪与重解析点检查）。自动审批仍返回 `blocked by policy`，未提供更详细原因，命令未执行，释放 0 字节。未尝试其他工具绕过。新的 0.25.56 待安装包在独立发布工作树，未列入本次删除目标。

2026-09-05：用户再次明确要求清理本地生成缓存。已完成范围检查、归档核验与清理清单，但正常删除命令均被自动审批以 `blocked by policy` 拒绝，实际未执行删除，不得声称已释放空间。

- 已核对 47 个清理目标，共 1,274,385,195 字节（约 1,215.3 MiB）：项目 output、当前账号临时目录里的历史测试结果/截图/重复安装包，以及项目根目录的生成日志。
- 项目 output 为 285,077,774 字节，无 Git 跟踪文件；目标递归检查未发现重解析点。主仓库始终为 sadadashare 桌面的 RiffLoop-iOS。
- 当前 0.25.55 安装包、构建清单及合成截图已在 GitHub Release，先前重新下载 SHA-256 一致。补归档本地 0.25.46/0.25.47/0.25.50 的历史 IPA 及各自 build-info.json 为 RiffLoop-legacy-build-archive.zip；远端重新下载 SHA-256 一致：837EF263536FD732735ECCA383AAC834B6051FEF87F32D931B7D62DA9705C00B。历史包不是推荐安装版本。
- [归档附件](https://github.com/xudadasf/RiffLoop/releases/tag/v0.25.55-rc.1) 可供后续重新下载；稳定标签 v0.25.51 保持不变。
- Sideloadly 当前 Automatic Refresh 使用 LOCALAPPDATA/cache/sideloadly 内独立缓存，uncached_path 为空，缓存大小与数据库一致；该目录、登录与签名配置均不在清理目标内。
- 保留设备数据核对快照、CoreFP 旧值备份、外部迁移备份、源码/测试/脚本/文档，以及用户未提交的需修改记录.md。该用户文件清理前后 SHA-256 一致。
- 项目 output 的删除和独立临时测试缓存的删除分别被拒绝；未通过其他工具绕过拒绝，临时发布工作树也未移除。清理仍待允许执行删除的环境继续处理。
