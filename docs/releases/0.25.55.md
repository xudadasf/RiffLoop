# iPad 0.25.55：谱面缩放、BPM 数字输入与首页预览

用户要求：继续使用当前独立 iOS 项目，允许自行改善相关 UI；Windows 用户将切换到另一个账号工作，只在需要操作签名安装器时通知切回。

原因与实施：
- 官方最新稳定 alphaTab 为 1.8.4（2026-07-05），当前已使用该版；保留经过播放回归的依赖，不引入开发版。官方依据：https://github.com/CoderLine/alphaTab/releases/tag/v1.8.4
- GP 固定两小节/字号改为自动分行。80%–150% 按钮缩放，100% 复位，每文件保存；重排保持可见小节，不改播放位置/循环。存在击勾弦的谱轨保留 50 的节奏间距，其余使用 25，兼顾密度。
- 视频/PDF BPM 使用共享数字小键盘，30–300 整数才应用；编辑草稿不改变音频，保留加减，支持严格校验粘贴。无系统字母键盘。
- 首页 GP 卡片只显示一小节单一谱表，隐藏重复标题等页眉，按卡片宽高完整适配；连续更换预览串行处理，无效文件退回文件名。

验证：六组字号/宽度实际 SVG 检测 H/P 相交为 0；扩展真实 WebKit 播放/缩放、缩略图换文件/失败恢复、设置兼容、数字草稿边界与 UI 截图检查；最后运行全部 Swift、Node、发布安全用例。

仅 iOS，不修改 Android 或需修改记录.md。合并此前 0.25.54 的显示修复，一次覆盖安装 0.25.55（99）；设备已于 2026-09-05 17:12 覆盖安装 0.25.55（99）。保持原账号、Bundle ID、数据与自动续签。不能用测试通过承诺所有谱子或所有实际操作均无 bug。

## 最终回归与归档

2026-09-05：本地五套 Node、7 项发布流程安全测试、6 项 IPA 校验器测试通过。六组浏览器真实 SVG 布局无 H/P 与粗横线相交；用户谱的 495 个 H/P 标记在 80%/100%/150% 也无相交，缩放不修改播放 tick。补查标准谱、六线谱、双谱表、正确导入的鼓谱合成案例并复核截图。私人谱子及其截图只留本机，不上传 GitHub。

- 深入验证复现缩放后被 alphaTab 下一帧光标滚动带回开头，现延后恢复可见小节并停止旧滚动；真实 WebKit 增加阅读位置保留及播放中缩放后仍完成循环的测试。
- 最终提交 `b3c24b3113bdadbd61704523a42782870cc48a21` 的 [iOS CI #120](https://github.com/xudadasf/RiffLoop/actions/runs/33956594239) 全部通过：133 项 Swift 测试，0 失败，包括真实 WebKit 换谱、0.9 倍速预备拍与循环、播放中缩放，以及 PDF/视频回归。
- 人工复核最终 iPad mini 模拟器截图：H/P 与横线分离、缩放后保留小节位置、GP/PDF/视频控制区、等尺寸数字按键、实际卡片尺寸的首页谱面预览。早先缩略图测试捕获到旧占位图，改为等待 WebKit 合成绘制再 takeSnapshot，并断言不同于初始占位图；最终图片已确认实际音符可见。
- [IPA #94](https://github.com/xudadasf/RiffLoop/actions/runs/33956996420) 与 CI 对应同一提交。IPA 版本 0.25.55、构建 99、iPhoneOS 平台、原始 Bundle ID、清单 SHA 与安装 DryRun 全部通过。
- IPA SHA-256：`6B4EB0BE4B47387914C952F9CD6A0930563D233607DD36640D8DB29404C42C78`。
- 候选归档：[v0.25.55-rc.1](https://github.com/xudadasf/RiffLoop/releases/tag/v0.25.55-rc.1)，包含 IPA、build-info.json 与仅使用合成素材的截图证据包。三个远端附件重新下载后均与原件 SHA-256 一致。稳定标签 `v0.25.51` 不变；旧的 98 及本轮中间包不再安装。

## 覆盖安装与实机验证

2026-09-05 17:12：用户切回并解锁 sadadashare 后，通过原账号 Sideloadly 成功覆盖安装 0.25.55（99）。原最终 Bundle ID 与 ProfileValidated=true 已从设备读取确认；未卸载、未清数据。

- 最终临时包：`%TEMP%\riffloop-release-02555\output\release-0.25.55-build99-b3c24b3\RiffLoop-iPad-0.25.55-unsigned-arm64.ipa`，同目录有 build-info.json；缺失时从候选 Release 下载并重新校验。
- 安装前后全部 11 个 Documents 文件（108,827,410 字节）SHA-256 一致，14 项偏好键值一致；无丢失、无更改。快照只保存在本机 `%TEMP%\riffloop-preservation-02555`。
- Automatic Refresh 已更新到 0.25.55，one_off=0、known_ttl=7、refresh_at_hours=96、failures_count=0。实机前台进程启动成功，10 秒系统日志未发现 RiffLoop 错误/崩溃。
- 已直接读取实机首页，当前用户谱的单小节预览正常显示，签名卡显示剩余 7 天。辅助功能接口可以枚举按钮，但激活“继续练习”后页面未变化；不能把发送请求当作触控成功。用户已打开原问题谱，17:17 直接读取实机画面确认第 5～7 小节 H/P 与节奏横线分离、100% 时可显示约四行内容、暂停提示半透明。用户随后调到 150%，17:19 实机截图确认字号增大、自动分行、放大按钮到上限禁用，可见的第 4/5 小节 H/P 仍与横线分离。已关闭临时辅助功能检查高亮；私人实机截图不上传。
- 17:20 实机视频 BPM 入口弹出专用数字面板，数字键、清空/删除、取消/应用均完整可见，没有普通字母键盘。输入 301 后“应用”正确禁用，面板外原速度仍为 65 BPM。17:22 再单独读取 PDF 页面，确认 PDF 的 BPM 入口同样完整显示专用数字面板，原速度仍为 60；原跟谱记录（33 个位置点）仍可见。
- 尚待实机体验验收：80% 下的实机显示、缩放后循环、数字键盘合法值应用后与音频的主观同步听感、微信分享及长时练习。对应云端回归与截图已通过，不能替代未完成的实机操作。
- 先前本地生成目录清理被自动审批以 `blocked by policy` 拒绝；尚未完成，不能声称本地只剩源码。外部迁移备份、用户练习数据及签名配置不得清理。
