# iPad 候选版 0.25.54（98）

后续需求与本版修复已合并到 [0.25.55（99）](0.25.55.md)。优先接续 0.25.55 的验证与覆盖安装，不再先安装 98；本页保留此前候选的验证证据。

状态：GP 技法标记显示修复，云端完整回归、IPA 构建及校验通过，覆盖安装待完成。当前实机仍为 0.25.53（97）；稳定标签 v0.25.51 不变。

跟踪：[Issue #2](https://github.com/xudadasf/RiffLoop/issues/2)。

## 问题与最小修复

- 用户实机看到第 5～7 小节的 H/P 击勾弦标记压在节奏横线上。已把当前文件复制到本机临时目录复现，原文件未改动，私人谱子及截图不上传 Git。
- alphaTab 1.8.4 的低音弦连奏标记朝下绘制，默认 rhythmHeight=25 留出的节奏区不足。只将练习页与文件预览的 rhythmHeight 调整为 50，为技法标记和横线留出间距；不修改库、音符、技法信息或播放数据。
- 按用户要求，右上角状态提示改为半透明背景（55% 黑色、白字），并允许触控穿过提示落到谱面。

## 回归证据

- 当前私人谱子的本机浏览器复现：原布局检测到 36 处 H/P 与节奏横线相交，增加间距后为 0。
- 新增真实 WebKit 回归：用公开合成的六弦、十六分/三十二分/六十四分音符案例，检查全部 174 个 H/P 标签与节奏图形的实际 SVG 几何边界；覆盖宽度 768/1133、练习字号 0.82/预览字号 1.0。
- 本机 Chrome 验证：旧布局在两种宽度下均有 45 处相交。间距 45 在较大字号/宽谱面时仍有相交，最终间距 50 才全部为 0，标签和节奏横线数量不变。未使用用户谱子作为云端测试素材。
- [iOS CI #115](https://github.com/xudadasf/RiffLoop/actions/runs/33953583026)：128 项 Swift 测试全部通过，新 WebKit 几何回归通过（6.865 秒）；原有 GP 换谱、0.9 倍速循环/预备拍恢复、PDF 和视频回归通过，Node 与发布安全检查通过。已复核合成谱、GP/PDF/视频页面截图。
- [IPA #91](https://github.com/xudadasf/RiffLoop/actions/runs/33954130603)：与 CI 对应同一应用提交 `1d47a4b1c927c520b833392b19f5f4d4fb4676e8`。IPA 内版本、构建号、平台、Bundle ID、清单提交和 SHA-256 校验，以及安装 DryRun 通过。
- IPA SHA-256：`53228A1FDC633D3AA83B9A3992FB615DE86B130C8BBCA2EDC521D9CAB2F59BDB`。
- 候选归档：[v0.25.54-rc.1](https://github.com/xudadasf/RiffLoop/releases/tag/v0.25.54-rc.1)，包含 IPA、build-info.json 和仅使用合成素材的截图证据包。稳定标签不变。

## 接续覆盖安装

2026-09-05 安装前快照读取 11 个 Documents 文件（108,827,410 字节）及 14 个偏好键。尚未安装 98，也尚未完成实机显示验收。

Windows 原生窗口自动化当前无法访问交互桌面：截图报 `IGraphicsCaptureItemInterop.CreateForMonitor ... 0x80070057`，点击报 `GetCursorPos ... 拒绝访问 0x80070005`；刷新窗口和重建工具连接后仍失败。账号权限/CoreFP 问题此前已经解决，这次尚无证据说明签名或 Apple 登录再次失败。需要用户恢复当前 Windows 账号的可交互桌面后继续，禁止盲点或卸载应用。

- 临时安装包：`%TEMP%\riffloop-release-02554\output\release-0.25.54-build98-1d47a4b\RiffLoop-iPad-0.25.54-unsigned-arm64.ipa`，同目录有清单；也可从上述 Release 下载。
- 临时数据核对脚本：`%TEMP%\riffloop-verify-preservation-02554.py`，快照目录 `%TEMP%\riffloop-preservation-02554`。真正安装前重新读取 `before`，安装后读取 `after` 比较全部文件 SHA-256 与偏好值；不要沿用更早快照覆盖新练习变化。
- 覆盖安装成功后读取设备版本 0.25.54 / 98、原最终 Bundle ID、签名有效性及 Automatic Refresh 登记。打开用户当前 GP，重新截图检查第 5～7 小节与半透明提示，记录实机验收结果。
- 本地生成目录清理此前被自动审批拒绝，尚未完成；不能声称本地仅剩源码。

## 安装约束

只覆盖安装，保留原 Apple 账号与 Bundle ID `com.riffloop.prototype.N3N672QNHY`。安装前后比较文件内容与练习设置，不卸载、不清数据。不修改 Android 或 `需修改记录.md`。
