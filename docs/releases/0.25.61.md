# 0.25.61 (105) — 预备拍停滞误判与换谱高亮异常

## 版本核对

2026-09-11 从 USB iPad mini 6 直接读取到 0.25.60（104）、iPadOS 27.0（24A5408d），原 Bundle ID 签名有效。当前账号桌面 checkout 停在 0.25.38（82），不得用作修复基线。

另一账号工作树的安装验收记录将 0.25.60（104）对应到 `39fb54c68adae7012a00fdaab9396939ea058197`，其最终 IPA 的五份 GP 资源与该提交一致。本轮直接读取安装包资源的 AFC 请求未成功，因此精确提交依据既有安装记录，设备本轮直接确认范围为版本、build、Bundle ID、签名。修复工作树为 `codex/ipad-playback-stall-20260911`。

## 全部异常线索

本机保留的只读采集目录：`C:/Users/sadada/Desktop/ipad 节拍器/output/diagnostics/ipad-20260911/`。素材与原始日志不提交 Git。

0.25.60 的会话 `35C74B97-B71F-4F00-8B20-6A5226669C0B` 有 20 条异常索引、0 个记录错误；已有 1916 条详细事件因上限轮转丢弃。索引存在不等于前后现场均完整。下列时间均为北京时间。

| 类别 | 记录与判断 | 本次处理 |
| --- | --- | --- |
| GP 播放停滞 | 9 月 10 日 22:12:17、22:14:22、22:16:09，均为交替拨弦；三组共 9 条关联记录。最后一组完整记录了暂停、seek、4 秒预备拍、播放请求后约 7 ms 即报停滞，音频上下文仍 running。 | 在正式播放请求处重置超时计时，覆盖 worker Playing 回执尚未到达的间隙。保留真实 3.5 秒无进度报警。 |
| 换谱 Script error | 9 月 9 日 22:06、22:28、22:42 及 9 月 10 日 22:26，各两条，共 8 条。file URL 未提供堆栈；最后一组在新谱 renderFinished 附近发生。 | 原始五份 GP 文件的 Chromium 播放/暂停/换谱重放复现 `Oy → highlightPlaybackRange → Lw` 读取缺失 realBounds。清除高亮同时释放旧起止音符；新谱载入前清除播放器高亮；坐标未就绪时延后高亮。该复现符合日志时序，不能据此证明八条匿名错误全部同因。 |
| WebContent 终止 | 9 月 10 日 22:07:07，一条；记录只有进程终止通知，没有终止原因。之后新页面恢复并继续播放。 | 保留已有有限恢复路径并回归。没有对应时刻系统报告，根因未确认，不标记修复。 |
| 启动短暂停顿 | 9 月 9 日 11:22:10，一条，约 0.846 秒。 | 单次启动时主线程延迟，缺少堆栈，未归因或修改。 |
| 上一会话未正常结束 | 9 月 9 日 11:22:09，一条，关联 0.25.59 会话。 | 仅 unknown，可由强退/系统回收等引起，不能当成 0.25.60 闪退。 |

另提取到 9 月 11 日 09:41:28 的 Jetsam：被终止的是 `appstored`（per-process-limit），RiffLoop 为 suspended 且没有终止原因标记；不能用于解释前一晚 WebContent 终止。

## 修改与验证

- 生产运输控制器 + 可控 worker 回执时序：修复前在 4 秒预备拍结束后 7 ms 失败，修正后通过；真实超时仍暂停且只报一次。
- 随包 alphaTab 的实际高亮清除/重渲染方法：修复前仍重画旧谱选区，修正后不再重画；缺失坐标不抛错。
- Chromium 使用原始五份 GP、原生产播放器、原音色和 worker，模拟原生设置回放及播放/暂停/选区/换谱，15 次连续切换：修复前复现 realBounds 错误，修复后 0 错误，播放器就绪且有非 seek 位置推进。
- 本地五套 Node 回归、7 项发布安全场景、6 项 IPA 校验测试通过。
- 增强现有 iPad WebKit 集成测试：监听脚本错误、换谱后检查残留错误、60 BPM 的 4 秒预备拍暂停/seek/恢复连续三轮。
- Windows Playwright WebKit 可渲染谱面，但该构建不支持播放器所需 Web Audio，不能作为音频回归通过证据。真实 WebKit/Swift 结果须以本提交云端 iPad 模拟器 CI 为准。

当前文档不代表已构建、已安装或实机验收通过。外接 Spark MINI 长时间练习和进程终止原因仍须后续实机证据。
