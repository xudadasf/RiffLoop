# 稳定使用期检查

基线：0.25.51（95），用户已确认本轮功能修正。下列项目是额外稳定性验证，不代表有已知未修复功能缺陷。

## 自动化覆盖

| 项目 | 入口 | 能证明什么 |
|---|---|---|
| 视频 1.0×、1.25× 各 50 轮 | `PracticeViewModelTests.testFiftyVideoLoopsAtTwoSpeedsRemainControllable` | 真实 H.264/AVPlayer 连续短区间循环完成，暂停后不恢复 |
| PDF 带音频 50 次实际回跳，再发送中断事件 | `PdfPracticeViewModelTests.testFiftyAudioFollowLoopsThenInterruptionStopsTransport` | 末尾循环不失效；收到音频中断通知后停止 |
| GP 50 轮预备拍握手 | `scripts/test-gp-playback.mjs` | 同一生产状态机反复等待 Paused/A 回执/速度回执；重复回调不双重启动，取消后不再启动 |
| 文件恢复 | `RecentProjectsStoreTests` 和各模式 profile 测试 | 模式隔离、重新创建存储后的恢复、无效文件回退 |

上述为短循环压力/状态验证，不等于 30 分钟真机音频、耗电、温度或蓝牙测试。人工发送中断通知也不等于真实锁屏或系统音频路由切换。

## 真机补充清单（尚未逐项自动验证）

每项记录「通过 / 失败 / 未测」。没有可靠触控或音频采集通道时，不把脚本无报错算作通过。

- [ ] 三种模式各正常练习约 30 分钟，记录是否卡顿、异常发热、声音逐渐偏移。
- [ ] 播放/循环/预备拍期间切到其他 App，再返回：应暂停或保持明确状态，不出现双重播放；手动恢复后定位正确。
- [ ] 播放/预备拍期间锁屏再解锁：无残留播放、无意外自动恢复，手动播放仍正常。
- [ ] 扬声器 → 蓝牙耳机 → 扬声器：音量/路由符合预期，暂停有效；单独记录蓝牙固有延迟，不能直接判为循环漂移。
- [ ] 退出并重启 App：三个模式分别恢复最后有效文件、练习设置与位置，不自行播放。
- [ ] 原文件删除或移走后重启：不崩溃、不无限重试，可重新选文件。

回报记录：版本、模式/文件、输出设备、速度、是否预备拍/循环、操作顺序、运行时长、实际现象。出现问题先诊断，不重写稳定 GP 循环状态机。
