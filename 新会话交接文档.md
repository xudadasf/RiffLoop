# RiffLoop 新会话交接

更新：2026-08-29。当前进入稳定使用与发布收尾，不再按旧的“GP 阶段尚未完成”继续开发。

## 当前结论

- 用户确认此前首页、视频、GP、PDF 问题已经修改优化完毕。
- 已安装并验收：0.25.51（95）；稳定标签 `v0.25.51` → `c233d88c4938cea8acecaad6a63034243d826dc2`。
- 2026-08-29 又发现首页工具栏把续签文字折叠成纯盾牌图标。候选版 0.25.52（96）已将“签名与续签”移到主页正文，显示准确到期时间、剩余天数和明确入口；[修正及真机验证记录](docs/releases/0.25.52.md)。已覆盖安装，等待用户体验确认后再决定是否设为新稳定标签。
- iOS CI #102：117 项 Swift 测试通过；IPA #87 对应同一提交。版本、SHA256、归档位置和已解决清单见 [稳定发布记录](docs/releases/0.25.51.md)。
- 开发分支：`codex/ipad-home-ui`。当前工作树在 `C:\Users\sadada\.codex\worktrees\4171\ipad 节拍器`；主工作区在 `C:\Users\sadada\Desktop\ipad 节拍器`。

## 不变量与保护

- 用户明确授权 iPad/iOS 工作，覆盖主工作区 AGENTS.md 中较早的 Android-only 范围。不要修改 Android，也不要擅自改写 AGENTS.md。
- 禁止触碰、暂存或提交用户保留的 `需修改记录.md`；其他用户脏文件也必须保留。逐个暂存本次文件。
- GP 保持 B → Paused → seek A → A 点回执 → 一小节预备拍 → A 到 B。
- 不恢复 WKWebView Blob/data URL 内嵌伴奏播放方案；原生伴奏路径保持稳定。
- 行为问题先诊断、补真实复现，再做最小修正；不做无关重构。
- 覆盖安装必须保持原签名账号和 Bundle ID，不卸载或清除应用数据。

## 本轮收尾

1. 稳定标签和安装包持久归档已完成。
2. 发布脚本改为当前分支、精确提交的 CI 门槛；下载和安装增加实际 IPA 元数据及哈希校验。操作以 [发布工作流](docs/RELEASE_WORKFLOW.md) 为准，不再照搬旧文档的固定分支。
3. 增加连续循环与中断回归；验证边界及真机补充项目见 [稳定性清单](docs/IPAD_STABILITY_CHECKS.md)。脚本/文档/测试收尾不改变已验收的 App，不需要为这些改动重新侧载。
4. 收尾验证已完成：提交 `dfcde833827baa2723bf45ef90b8f77f5505177a` 的 CI #103 成功，119 项 Swift 测试、5 套 Node 回归和发布脚本安全测试通过；视频两档各 50 轮、PDF 50 次回跳、GP 50 轮握手均通过。IPA #88 同提交构建、下载、包内校验及安装 DryRun 通过，未安装。后续文档提交只记录这些结果。

## 后续开始顺序

读取本文件、可用的 AGENTS.md 和 `docs/agents/domain.md`，检查 git 状态及当前分支。当前工作树若缺少后两份配置，读取主工作区的同名文件，不将缺失视为开发阻塞。

先确认用户是否反馈了新缺陷；没有新问题时维持稳定基线，按真实练习反馈决定下一轮，而不是继续扩展功能。所有新测试结果必须区分真实播放器、模拟回调、真机触控及主观听感。
